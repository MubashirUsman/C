#include <stdio.h>
#include <stdlib.h>

int main(void){
    typedef struct
    {
        int data;
        struct node *address;
    } node;
    node *head, *middle, *end;
    head = malloc(sizeof)

    
    return 0;
}