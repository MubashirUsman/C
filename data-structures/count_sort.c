#include <stdio.h>
#include <stdlib.h>

void count_sort(int* sequence){
   int times=0;
   int unique_nums[] = {NULL};

   for(int i=0; i<size; i++){
      if (*(sequence+i) )
   }
}

int main(){
   int sequence[] = {0, 10, 2, 4, 2, 1, 0, 3, 4, 10};

}
